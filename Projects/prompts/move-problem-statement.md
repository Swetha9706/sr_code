# Task: Two scoped edits to the Krētha case study draft

## Before you start
Read `CLAUDE.md`, `DESIGN.md`, and `PRODUCT.md` first.

Work only on `kretha.html` (the in-progress draft). Do not touch `kretha_copy.html` (the live version).

Do these as two separate, independent edits. Confirm each one before moving to the next.

---

## Task 1: Reorder the problem-statement block

Find the section that opens with the eyebrow "What problem am I solving?" It contains, in order: the statement "Procurement here is rarely a single-screen task..." a body paragraph after it, and the three "reasons" cards (Fragmented coordination, Repetition without infrastructure, Organisational complexity).

Find the section that opens with the eyebrow "How did I get to the problem?" It contains the market-opportunity block and the three-actors block (Retailers, Wholesalers, Manufacturers).

Move the "What problem am I solving?" content (statement, body paragraph, and the three reason cards) so it comes **after** the market-opportunity and three-actors content, not before it. The statement and its three reason cards should stay together, immediately adjacent, with the statement first.

Everything else in both sections stays exactly as it is: no rewording, no rewritten copy, no other reordering, no changes to the findings/map/connected-system content that follows.

## Task 2: Remove the redundant findings card trio

Find the block titled "Three findings that shaped everything after" (or similar) — three cards reading roughly "Procurement is collaborative," "Repeat ordering is critical," and "Business users need control."

Find the finding→decision map that follows it later in the page — the table-style block pairing each research finding with the design decision it led to.

The three cards restate what the map already says. Delete the three-card block entirely. Leave the finding→decision map untouched. Do not merge their wording into the map or anywhere else — just remove the cards.

---

## If the file doesn't match this description

If you open `kretha.html` and the current structure doesn't match what's described above for either task (different heading text, content already reordered or already removed, blocks split differently), stop and describe what you found instead of guessing at the change.

## Constraints
- Task 1 is reposition only. Task 2 is deletion only. No rewording anywhere, no other reordering, no touching images, alt text, or any section not named above.
- Do not fabricate any stat, count, or credential.
- Confirm the diff for each task before considering it done.
